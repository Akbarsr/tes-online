<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /tes-online/

    # Jika request bukan file/folder yang ada
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Arahkan ke index.php
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
